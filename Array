Array of objects

- First use new keyword to create the array

- The use new keyword to create objects you wanna put in the array

Example:

Dog [] dogs = new Dog[2];
dogs[0] = new Dog(8);
dogs[1] = new Dog(20);
dogs[0].makeNoise();

After execution:

dogs = [Dog(8), Dog(20)]
          0       1
