/* compareTo java's method for comparing */

public interface Compare {
	/* Returns negative if this is less than o
	   Returns 0 if equal
	   Returns positive if this is greater than o
	*/
	 public int compareTo(Compare o);
}